<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zenith.admin.login.dao.AdminLoginDao">

	<select id="selectAdminInfo" parameterType="hashmap" resultType="hashmap">
		SELECT	U_ID		,PHONE		,EMAIL		,USER_TYPE	
				,DATE_FORMAT(UPDATE_DATE, '%Y-%m-%d %H:%i:%s') AS UPDATE_DATE
				,DATE_FORMAT(CREATE_DATE, '%Y-%m-%d %H:%i:%s') AS CREATE_DATE
				,DATE_FORMAT(LOGIN_DATE, '%Y-%m-%d %H:%i:%s') AS LOGIN_DATE
				,UPDATE_ID 
		FROM 	USER_INFO 
		WHERE 	USER_TYPE = 'C010100'
		AND 	U_ID = #{uId}
		AND		USER_PW	= #{userPw}
	</select>

	<update id="updateLogin" parameterType="hashmap">
		UPDATE USER_INFO SET LOGIN_DATE = NOW()
		WHERE 1=1 
		AND U_ID = #{uId}
	</update>

</mapper>

