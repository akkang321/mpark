<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zenith.common.dao.CodeDao">
	
	<select id="getCode" parameterType="String" resultType="hashmap">
		SELECT	CODE_ID			,CODE_NAME		,PARENT_CODE	,CODE_DESC
				,ETC1			,ETC2			,ETC3			,CREATE_ID
				,CREATE_DATE	,UPDATE_ID		,UPDATE_DATE
		FROM 	CODE_INFO
		WHERE PARENT_CODE = #{parentCode}
	</select>
	
	<select id="getMyInfo" parameterType="String" resultType="hashmap">
		SELECT	U_ID		,USER_NAME		,PHONE		,EMAIL		,GENDER		,USER_STATE
		FROM 	USER_INFO
		WHERE U_ID = #{uId}
	</select>
	
</mapper>

