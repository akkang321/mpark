package com.mpark.admin.login.service;

public class AdminDetailsHelper {

	/**
	 * 인증된 사용자객체를 VO형식으로 가져온다.
	 * @return Object - 사용자 ValueObject
	 */
	/*
	 * public static Object getAuthenticatedUser() { return ( Map<String,
	 * Object>)RequestContextHolder.getRequestAttributes().getAttribute("userInfo",
	 * RequestAttributes.SCOPE_SESSION)==null ? new AdminVO() : ( Map<String,
	 * Object>) RequestContextHolder.getRequestAttributes().getAttribute("userInfo",
	 * RequestAttributes.SCOPE_SESSION);
	 * 
	 * }
	 */
}
